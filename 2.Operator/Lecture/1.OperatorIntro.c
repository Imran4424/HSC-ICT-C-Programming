#include <stdio.h>


int main()
{
    printf("\tOperator Intro\n");

    return 0;
}

/**
 What is operator?
    a + b. here, + is an operator
    a - b. here, - is an operator
    a * b. here, * is an operator

 what is operand?
    a + b, operator: +, operand: a, b
    3 + 2, operator: +, operand: 3, 2

    a - b, operator: -, operand: a, b
    3 - 2, operator: -, operand: 3, 2

 Operators based on their work

 Arithmetic Operator
    + (plus)
    - (minus)
    * (product or multiply)
    / (division)
    % (modulo) - remainder after division

 Relational Operator
    > (greater than)
    >= (greater than equal)
    < (Less than)
    <= (Less than equal)
    == (equal)
    != (not equal)

 Assignment Operator
    = (assignment operator)

 Logical Operator
    || (logical OR)
    && (Logical AND)
    ! (NOT)

 Bitwise Operator (similar to logic gate)
    | (Bitwise OR)
    & (Bitwise AND)
    ^ (Bitwise Exclusive-OR (X-OR))
    << (Bitwise Left Shift)
    >> (Bitwise Right Shift)

 Increment and Decrement Operator
    ++ (plus plus)
    -- (minus minus)

 Conditional Operator
    ?: (Conditional Operator)
*/
